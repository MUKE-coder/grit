# Scaffold output — regenerated with `grit new` so it cannot drift from what the
# CLI actually emits today.
grit-bench/
grit-bench-pooled/

# Dependency and build output
*/node_modules/
*/vendor/
*/.next/
*/encore.gen/

# Run lock, and archived copies of superseded runs
.bench.lock
results/archive/
